# .env.example — Ejemplo de variables de entorno para todo el proyecto
# Copia este archivo a ".env" y modifica los valores sensibles antes de ejecutar.

# -----------------------------
# Entorno / App
# -----------------------------
NODE_ENV=development
# Puerto por defecto usado por algunos servicios (puedes sobrescribir en contenedores individuales)
PORT=3500
SERVICE_NAME=auth
HOSTNAME=

# -----------------------------
# Base de datos Postgres
# -----------------------------
DB_HOST=db
DB_PORT=5432
DB_NAME=retos_microservicios
DB_USER=postgres
DB_PASS=securepassword
# Schema usado por servicios que lo requieren
DB_SCHEMA=auth

# -----------------------------
# JWT / Autenticación
# -----------------------------
# Cambia por una frase/clave segura en producción
JWT_SECRET=mi_secreto_super_seguro
TOKEN_EXP=1h

# -----------------------------
# Consul (registro de servicios)
# -----------------------------
CONSUL_HOST=consul
CONSUL_PORT=8500

# -----------------------------
# RabbitMQ (messaging)
# -----------------------------
# Se soporta tanto URL completa como partes individuales
RABBITMQ_URL=amqp://admin:securepass@rabbitmq:5672
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=securepass
RABBITMQ_VHOST=/
# Exchange usado para eventos de auth
AUTH_EVENTS_EXCHANGE=auth.events

# -----------------------------
# Twilio (SMS)
# -----------------------------
# Dejar vacíos si se usan mocks en tests
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+10000000000

# -----------------------------
# Logging
# -----------------------------
# Valores típicos: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO
